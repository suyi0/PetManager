{
  "name": "petmanager-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "desktop/main/main.cjs",
  "scripts": {
    "desktop:test": "node --test desktop/main/*.test.cjs pethospital/frontend/vue.config.test.cjs",
    "desktop:prepare:dev": "npm --prefix pethospital/frontend run build && bash bin/build.sh",
    "desktop:prepare:client": "VUE_APP_DESKTOP_CLIENT=true npm --prefix pethospital/frontend run build",
    "electron:dev": "npm run desktop:prepare:dev && PETMANAGER_DESKTOP_MODE=dev electron desktop/main/main.cjs",
    "electron:client": "npm run desktop:prepare:client && PETMANAGER_DESKTOP_MODE=client electron desktop/main/main.cjs"
  },
  "dependencies": {
    "axios": "^1.11.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "electron": "^37.2.3",
    "@vitejs/plugin-vue-jsx": "^5.0.1"
  }
}
